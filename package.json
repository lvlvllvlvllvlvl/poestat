{
  "name": "poestat",
  "module": "src/index.ts",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "sideEffects": false,
  "devDependencies": {
    "@types/bun": "latest",
    "json-schema-to-typescript": "^14.0.4"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "bun build src/*.ts --outdir=dist --splitting"
  },
  "files": [
    "src",
    "data",
    "dist"
  ]
}
