{
  "name": "poestat",
  "module": "ts/index.ts",
  "main": "dist/index.js",
  "types": "src/types/index.d.ts",
  "type": "module",
  "sideEffects": false,
  "devDependencies": {
    "@types/bun": "latest",
    "json-schema-to-typescript": "^14.0.4"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "wasm-pack build --target web && bun build ts/*.ts --outdir=dist --splitting"
  },
  "files": [
    "ts",
    "dist"
  ]
}
