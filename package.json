{
  "name": "poestat",
  "exports": "./ts/index.ts",
  "main": "dist/index",
  "types": "dist/index",
  "type": "module",
  "sideEffects": false,
  "devDependencies": {
    "@types/bun": "latest",
    "json-schema-to-typescript": "^14.0.4"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "tsc && wasm-pack build --target web && bun build ts/*.ts --outdir=dist --splitting"
  },
  "files": [
    "ts",
    "dist"
  ]
}
